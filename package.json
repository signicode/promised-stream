{
  "name": "promised-stream",
  "version": "1.0.0",
  "description": "A version of the standard \"stream\" library for implementation of Promise based streams",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL-3.0",
  "devDependencies": {
    "cucumber": "^2.0.0-rc.9",
    "scramjet": "^2.5.2"
  },
  "dependencies": {
    "debug": "^2.6.3"
  }
}
